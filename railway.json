{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "npm i -g pnpm && pnpm install && pnpm --dir artifacts/api-server build"
  },
  "deploy": {
    "startCommand": "pnpm --dir artifacts/api-server start"
  }
}
